# Cyber Slide Tools

AIを使ってGoogleスライドを自動生成・編集できるツールです。

## 使い方

1. リポジトリをクローン
2. Google Cloudの認証情報 (`credentials.json`) と Gemini APIキー (`.env`) を準備
3. フロントエンド起動:
   ```bash
   cd frontend
   npm install
   npm run dev
